# グループ管理機能

## 概要

登録項目をグループ分けし、一覧の管理・絞り込みをしやすくする機能。

## 前提（要確認事項）

1項目が所属できるグループは**1つ**とする（複数グループ所属は初期スコープ外）。
グループの階層化（親子グループ）も初期スコープ外とする。この前提が実際の要望と異なる場合、
本要件を見直すこと。

## EARS形式要件

When ユーザーがグループを作成し、登録項目にグループを割り当てたとき,
the system shall 項目をグループ単位で分類し、一覧をグループごとに表示・絞り込み可能にする,
グループ名の編集・削除、および項目のグループ変更が随時可能な状態を維持する。

## 詳細要件

- グループは名前(1〜30文字)のみを持つ分類単位とする（説明文・アイコンの付加情報は持たない）。
- 1項目が所属するグループは1つ（未所属も許容し「未分類」として扱う）。
- 一覧画面でグループ単位の絞り込み表示ができる。
- グループの作成・名称編集ができる。
- グループの削除ができる。**グループを削除すると、そのグループに所属する項目もすべて削除する**
  （未分類への退避は行わない）。削除操作は確認を経てから確定し、確定後は取り消し不可とする。

## ユースケース

1. ユーザーが新規グループを作成し、名前を入力する。
2. [[req-000001]] で登録済みの項目、または新規登録時に、所属グループを選択する。
3. 一覧画面でグループを選択すると、そのグループに属する項目のみが表示される。
4. グループ名を編集する。
5. 不要になったグループを削除する。削除確認を経て確定すると、そのグループに所属していた項目も
   すべて削除される。

## 技術的制約

- グループデータは chrome.storage.local に保存する（[[technical.yaml]]参照）。
- サイドパネルUI(002-side-panel-ui)でグループの読み書き・表示・管理を行う。

## エッジケース

- グループ削除時、所属していた項目はグループごとすべて削除される。削除は取り消せないため、
  削除操作には確認ダイアログを伴う。
- 同名グループの重複作成は許容する（識別はグループIDで行う）。
- グループを作成せずに利用する場合、全項目は「未分類」として一覧表示される（未分類自体は
  グループ削除の対象外の固定分類であり、削除できない）。

## 成功基準

- グループは最大50個まで作成可能。
- グループ絞り込み操作から一覧の再描画までが300ms以内に完了する。
- グループ削除操作は確認を経て確定し、確定後は旧所属項目の100%が一覧・保存データから削除される
  （未削除の項目は0件）。
- グループ名は1〜30文字でバリデーションされ、範囲外入力は100%エラーとなる。

## 参考資料

- [[req-000001]] 項目登録・保存機能
- [[req-000002]] 一覧表示・コピー機能(デフォルトマスク表示)
- [[req-000007]] 項目一括削除機能（同様に確認を経た削除・取り消し不可の方針を踏襲）

## 変更履歴

- v1.1: グループ削除時の挙動を「所属項目を未分類に戻す」から「所属項目も削除する」に変更
  （ユーザーからの明示的な要望に基づく仕様変更）。
